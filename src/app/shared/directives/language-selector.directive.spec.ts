import { LanguageSelectorDirective } from './language-selector.directive';

describe('LanguageSelectorDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageSelectorDirective();
    expect(directive).toBeTruthy();
  });
});
